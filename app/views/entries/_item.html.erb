<li class="flex justify-between items-center border-b border-b-border last:border-b-0  py-4 w-full ">
  <%= link_to habit_entry_path(id: entry, habit_id: entry.habit), data: { turbo: false }, class: "flex-grow"  do %>
      <div>
        <p class="font-medium"><%= entry.date.strftime("%d %B %Y") %></p>
        <p class="text-muted-foreground"><%= entry.date.strftime("%H.%M") %></p>
        <% if entry.description.present? %>
          <p class="text-muted-foreground"><%= "#{truncate(entry.description, length: 40)} • #{mood_label(entry.mood)}" %></p>
        <% else %>
          <p class="text-muted-foreground"><%= "#{mood_label(entry.mood)}" %></p>
        <% end%>
      </div>
  <% end %>
  <%= link_to habit_entry_path(id: entry, habit_id: entry.habit), data: { turbo: false }  do %>
    <%= lucide_icon("chevron-right", class:"text-muted-foreground")%>
  <% end %>
</li>